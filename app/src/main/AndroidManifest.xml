<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moransenyor.cashmap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddNewReceipt"
            android:label="@string/title_activity_add_new_receipt"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewReceipt"
            android:label="@string/title_activity_view_receipt"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EditReceipt"
            android:label="@string/title_activity_edit_receipt"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>